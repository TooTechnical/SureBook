import { desc, eq } from "drizzle-orm";import { db } from "@/db";import { customers } from "@/db/schema";import { requireSession } from "@/lib/session";
export default async function Page(){const s=await requireSession();const rows=await db.query.customers.findMany({where:eq(customers.salonId,s.salonId),orderBy:[desc(customers.updatedAt)]});return <><h1>Customers</h1><p style={{color:"var(--muted)"}}>Customer history helps your team spot repeat no-shows and serve regulars better.</p><div className="card table-wrap" style={{padding:12}}><table><thead><tr><th>Customer</th><th>Contact</th><th>Bookings</th><th>No-shows</th><th>Marketing</th></tr></thead><tbody>{rows.map(c=><tr key={c.id}><td><strong>{c.name}</strong></td><td>{c.phone}<br/><small>{c.email}</small></td><td>{c.totalBookings}</td><td>{c.noShowCount}</td><td>{c.marketingConsent?"Consented":"No consent"}</td></tr>)}</tbody></table></div></>}
