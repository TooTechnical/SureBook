import { eq } from "drizzle-orm";import { db } from "@/db";import { staff } from "@/db/schema";import { requireSession } from "@/lib/session";import { createStaffAction } from "@/actions/salon";
export default async function Page(){const s=await requireSession();const rows=await db.query.staff.findMany({where:eq(staff.salonId,s.salonId)});return <><h1>Staff</h1><div style={{display:"grid",gridTemplateColumns:"minmax(0,1fr) 380px",gap:20}}><div className="grid-auto">{rows.map(x=><article className="card" style={{padding:20}} key={x.id}><h3>{x.name}</h3><p>{x.email||"No email"}<br/>{x.phone||"No phone"}</p><span className="badge">{x.role}</span></article>)}</div><form action={createStaffAction} className="card" style={{padding:22,display:"grid",gap:14,height:"fit-content"}}><h2 style={{margin:0}}>Add team member</h2><label><span className="label">Name</span><input className="input" name="name" required/></label><label><span className="label">Email</span><input className="input" type="email" name="email"/></label><label><span className="label">Phone</span><input className="input" name="phone"/></label><button className="btn btn-primary">Add staff member</button></form></div></>}
