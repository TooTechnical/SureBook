import { eq } from "drizzle-orm";import { db } from "@/db";import { salons } from "@/db/schema";import { requireSession } from "@/lib/session";import { startStripeOnboardingAction,updateSalonAction } from "@/actions/salon";
export default async function Page(){const s=await requireSession();const salon=await db.query.salons.findFirst({where:eq(salons.id,s.salonId)});if(!salon)return null;return <><h1>Salon settings</h1><div style={{display:"grid",gridTemplateColumns:"minmax(0,1fr) 380px",gap:20}}><form action={updateSalonAction} className="card" style={{padding:22,display:"grid",gap:14}}><h2>Business details</h2><label><span className="label">Salon name</span><input className="input" name="name" defaultValue={salon.name}/></label><label><span className="label">Phone</span><input className="input" name="phone" defaultValue={salon.phone||""}/></label><label><span className="label">Address</span><textarea className="input" name="address" defaultValue={salon.address||""}/></label><label><span className="label">County</span><input className="input" name="county" defaultValue={salon.county||""}/></label><label><span className="label">Free-cancellation window (hours)</span><input className="input" type="number" name="cancellationWindowHours" defaultValue={salon.cancellationWindowHours}/></label><label><span className="label">Default deposit (€)</span><input className="input" type="number" step="0.01" name="defaultDeposit" defaultValue={(salon.defaultDepositCents/100).toFixed(2)}/></label><button className="btn btn-primary">Save settings</button></form><div><section className="card" style={{padding:22}}><h2>Stripe payments</h2><p style={{color:"var(--muted)"}}>{salon.stripeChargesEnabled?"Connected and ready to accept deposits.":"Connect your salon’s Stripe account to take deposits and receive payouts directly."}</p><form action={startStripeOnboardingAction}><button className="btn btn-primary">{salon.stripeAccountId?"Continue Stripe setup":"Connect Stripe"}</button></form></section><section className="card" style={{padding:22,marginTop:18}}><h2>Public booking page</h2><code style={{overflowWrap:"anywhere"}}>/book/{salon.slug}</code></section></div></div></>}
