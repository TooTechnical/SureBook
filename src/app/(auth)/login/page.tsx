import Link from "next/link";
import { Logo } from "@/components/Logo";
import { loginAction } from "@/actions/auth";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <main className="container" style={{padding:"42px 0"}}><Logo/><div className="card" style={{maxWidth:480,margin:"70px auto",padding:28}}><h1>Welcome back</h1><p style={{color:"var(--muted)"}}>Run today’s diary and protect every booking.</p>{q.error&&<p style={{color:"#a11"}}>The email or password was incorrect.</p>}<form action={loginAction} style={{display:"grid",gap:16}}><label><span className="label">Email</span><input className="input" name="email" type="email" required/></label><label><span className="label">Password</span><input className="input" name="password" type="password" required/></label><button className="btn btn-primary">Log in</button></form><p>New to SureBook? <Link href="/signup"><strong>Create an account</strong></Link></p></div></main>}
