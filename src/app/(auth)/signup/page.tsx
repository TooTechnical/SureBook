import Link from "next/link";
import { Logo } from "@/components/Logo";
import { signupAction } from "@/actions/auth";
export default async function Signup({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <main className="container" style={{padding:"32px 0"}}><Logo/><div className="card" style={{maxWidth:520,margin:"clamp(28px,6vw,50px) auto",padding:"clamp(22px,5vw,32px)"}}><h1>Create your salon workspace</h1><p style={{color:"var(--muted)"}}>You can configure staff, services and deposits after signup.</p>{q.error&&<p className="alert" role="alert">An account with that email already exists.</p>}<form action={signupAction} style={{display:"grid",gap:16}}><label><span className="label">Salon name</span><input className="input" name="name" autoComplete="organization" required/></label><label><span className="label">Business phone</span><input className="input" name="phone" type="tel" autoComplete="tel" required/></label><label><span className="label">Email</span><input className="input" name="email" type="email" autoComplete="email" required/></label><label><span className="label">Password</span><input className="input" name="password" type="password" autoComplete="new-password" minLength={8} aria-describedby="password-help" required/><small id="password-help" style={{color:"var(--muted)"}}>Use at least 8 characters.</small></label><button className="btn btn-primary">Create workspace</button></form><p>Already registered? <Link href="/login"><strong>Log in</strong></Link></p></div></main>}
